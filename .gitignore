# Dependencies & build
node_modules
dist
dist-ssr
dev-dist

# Vite
.vite

# TypeScript incremental build
*.tsbuildinfo

# Secrets — never commit Firebase keys or Google client secret
.env
.env.*
!.env.example
*.local

# Logs (Firebase CLI, etc.)
*.log
firebase-debug.log
firestore-debug.log
ui-debug.log

# OS
.DS_Store
Thumbs.db

# Editors (optional local settings)
.idea/
.vscode/*
!.vscode/extensions.json

# Cursor (local agent state — not for the repo)
.cursor/
